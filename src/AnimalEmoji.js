import React from 'react';

export default function AnimalEmoji(props) {
  return <span>
      {props.animal === 'whale' && '🐳'}
      {props.animal === 'whale' && '🐳'}
      {props.animal === 'whale' && '🐳'}
      {props.animal === 'whale' && '🐳'}
  </span>;
}
